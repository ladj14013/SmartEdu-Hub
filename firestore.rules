rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Rules will be built page by page from here.
    // WARNING: This allows anyone to read, write, and delete any data.
    // This should not be used in production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
