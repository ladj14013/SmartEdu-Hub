rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow read access to everyone for local development.
    // NOTE: This is not secure for production.
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null; // Allow writes only if logged in
    }
  }
}
