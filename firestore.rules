
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // قاعدة مؤقتة: اسمح بجميع عمليات القراءة والكتابة على جميع المستندات
    // سيتم استبدال هذه القاعدة بقواعد أكثر تحديدًا
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
